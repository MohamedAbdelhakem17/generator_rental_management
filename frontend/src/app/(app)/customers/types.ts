export interface CustomerRow {
  id: string;
  code: string;
  companyName: string;
  contactPerson: string;
  phone: string;
  taxNumber: string;
  address: string;
  active: boolean;
  createdAt: string;
}
